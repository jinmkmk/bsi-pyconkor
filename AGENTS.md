# AGENTS.md

이 문서는 이 저장소에서 작업하는 AI 코딩 에이전트를 위한 기본 지침입니다.
더 구체적인 하위 디렉터리의 `AGENTS.md`가 있다면 해당 범위에서는 그 지침을
우선합니다.

## 프로젝트 개요

이 저장소는 NEIS 공개 API를 활용해 초중고 급식 메뉴를 조회하고 분석하는
웹 애플리케이션을 단계별로 구현하는 워크숍입니다. 현재 저장소는 워크숍 문서와
API 명세 원본을 중심으로 구성되어 있으며, 프론트엔드, 백엔드 및 배포 코드는
워크숍을 진행하면서 추가될 수 있습니다.

## 일반 작업 지침

- 요청된 문제와 직접 관련된 범위만 변경하고 기존 동작을 보존합니다.
- 이 문서보다 저장소의 기존 코드, 설정, 문서 및 디렉터리별 지침을 우선합니다.
- 변경 전에 관련 파일과 기존 구현 패턴을 확인하고 같은 구조와 명명 규칙을
  따릅니다.
- 단순하고 명확한 구현을 선호하며, 현재 요구사항에 필요하지 않은 추상화,
  리팩터링 또는 의존성을 추가하지 않습니다.
- 생성된 파일이나 외부 데이터 원본은 필요한 경우가 아니면 직접 수정하지
  않습니다.
- 동작이나 설정이 달라지면 관련 문서와 예제를 함께 갱신합니다.

## Python 가이드라인

- 공개 함수, 메서드 및 데이터 구조에 명확한 타입 힌트를 사용하고 타입 검사를
  우회하는 광범위한 `Any`나 불필요한 타입 캐스팅을 피합니다.
- 예상 가능한 오류를 구체적으로 처리하고, 예외를 무시하거나 원인을 숨기는
  포괄적인 예외 처리를 추가하지 않습니다.
- 입력값과 외부 API 응답은 신뢰하지 말고 경계에서 검증합니다.
- 표준 라이브러리와 기존 의존성을 우선 사용합니다. 새 의존성이 꼭 필요하면
  프로젝트의 기존 패키지 관리 방식으로 선언하고 선택 이유를 명확히 합니다.
- 기존 포매터, 린터 및 타입 검사 설정을 따르고 읽기 쉽고 작은 단위의 코드를
  작성합니다.

## TypeScript 가이드라인

- 엄격한 타입을 유지하고 `any`, 불필요한 타입 단언 및 타입 검사 비활성화를
  피합니다. 외부 데이터는 런타임 검증 후 구체적인 타입으로 다룹니다.
- 오류를 조용히 삼키거나 성공 값으로 위장하지 말고 호출자가 처리할 수 있는
  형태로 명확히 전달합니다.
- 기존 모듈, 유틸리티 및 컴포넌트 패턴을 재사용하고 중복 구현이나 성급한
  공통화를 피합니다.
- 새 패키지는 기존 패키지 관리자와 잠금 파일을 사용해 추가하며, 동일한 기능의
  기존 의존성이 있는지 먼저 확인합니다.
- 프로젝트의 컴파일러, 포매터 및 린터 설정을 준수하고 타입 안정성을 낮추는
  우회책을 사용하지 않습니다.

## 테스트 및 유효성 검사

- 동작을 변경하면 정상 흐름과 주요 실패 흐름을 검증하는 테스트를 추가하거나
  수정합니다. 버그 수정에는 가능한 경우 회귀 테스트를 포함합니다.
- 변경 범위를 검증하는 가장 작은 기존 테스트부터 실행한 뒤 필요할 때 범위를
  넓힙니다.
- 저장소에 실제로 구성된 포매팅, 린트, 타입 검사, 테스트 및 빌드 명령만
  사용합니다. 구성 파일이나 패키지 스크립트를 먼저 확인하고, 아직 구성되지
  않은 도구나 명령을 임의로 도입하거나 추정하지 않습니다.
- 문서만 변경한 경우 링크, 경로, 예제 및 Markdown 구조를 검토합니다.
- 실행하지 못한 검증이나 남아 있는 실패가 있다면 결과에 명확히 기록합니다.

## 보안

- 비밀, 자격 증명, 토큰, 개인정보 또는 민감한 운영 정보를 코드, 로그,
  테스트 픽스처 및 문서에 포함하지 않습니다.
- 사용자 입력과 외부 API 데이터를 검증하고 출력, 파일 경로, 명령 및 네트워크
  요청에 사용할 때 안전하게 처리합니다.
- 보안 검사를 비활성화하거나 인증 및 권한 검사를 우회하지 않습니다.
- 발견한 취약점은 공개 이슈에 작성하지 말고 `SECURITY.md`의 비공개 신고 절차를
  따릅니다.

## 문서화

- 문서는 현재 동작과 일치하도록 유지하고 명령, 파일 경로 및 설정 예제를
  실제 저장소 구조와 대조합니다.
- 코드가 스스로 설명되지 않는 경우에만 간결한 주석을 추가하고, 구현을 그대로
  반복하는 주석은 피합니다.
- 프로젝트 전체에 영향을 주는 변경은 필요에 따라 `README.md`, 관련 워크숍
  문서 및 변경 이력을 함께 갱신합니다.

## 주의사항 및 가드레일

- 사용자 변경이나 요청 범위 밖의 변경을 되돌리거나 덮어쓰지 않습니다.
- 명시적인 요청 없이 파괴적인 Git 명령, 대규모 파일 삭제, 데이터 마이그레이션
  또는 호환성을 깨는 변경을 수행하지 않습니다.
- 테스트 통과를 위해 검증을 약화하거나 실패를 건너뛰거나 제품 코드를
  테스트에 맞춰 하드코딩하지 않습니다.
- 불명확한 요구사항이 보안, 데이터 손실, 공개 API 또는 사용자 경험에 큰 영향을
  줄 수 있다면 구현 전에 확인합니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고 `CONTRIBUTING.md`에 정의된
  Conventional Commits 형식으로 작성합니다.
- 관련 없는 파일을 함께 커밋하지 않으며 생성물, 로컬 설정 및 비밀이 포함되지
  않았는지 확인합니다.
- Pull Request는 하나의 목적에 집중하고 변경 이유, 주요 구현 내용, 수행한
  검증과 알려진 제한 사항을 명확히 설명합니다.
- `.github/PULL_REQUEST_TEMPLATE.md`가 있으면 해당 구조와 체크리스트를
  사용하며, 관련 이슈를 `Closes #<번호>` 형식으로 연결합니다.
- 리뷰 가능한 크기의 변경을 유지하고 리뷰 의견에는 코드나 문서 변경으로
  응답합니다.
