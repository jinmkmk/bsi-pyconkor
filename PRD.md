# 급식 배틀 - 학교 급식 조회 앱 PRD

## 1. 문서 목적

이 문서는 NEIS 공개 API를 이용해 학교별 중식 메뉴를 조회하는 웹
애플리케이션의 제품 요구사항을 정의한다. 제품과 구현의 범위를 판단하는 기준은
이 문서이며, 기술적인 구현 방법은 `TRD.md`에서 정의한다.

## 2. 제품 개요

### 2.1 배경과 목적

학생, 학부모, 교직원은 학교 급식을 확인하기 위해 학교별 홈페이지나 교육청
서비스를 찾아야 한다. 이 앱은 학교 이름의 일부만 입력해 학교를 찾고, 원하는
기간의 중식 메뉴를 한 화면에서 빠르게 확인할 수 있도록 한다.

### 2.2 대상 사용자

- 재학 중인 학교의 급식을 확인하려는 학생
- 자녀의 학교 급식을 확인하려는 학부모
- 학교 급식 일정을 확인하려는 교직원 및 일반 사용자

### 2.3 성공 기준

- 사용자가 학교 검색, 날짜 범위 선택, 급식 확인의 3단계를 완료할 수 있다.
- 동일한 학교명이 있을 때 지역과 학교 종류를 보고 올바른 학교를 선택할 수 있다.
- 결과가 없거나 요청이 실패해도 원인과 다음 행동을 이해할 수 있다.
- API 인증키는 브라우저에 노출되지 않는다.

## 3. 범위

### 3.1 포함 범위

- 학교 이름 일부를 이용한 학교 검색
- 검색 결과 중 한 학교 선택
- 시작일과 종료일로 조회 범위 선택
- 선택한 기간의 중식 메뉴를 날짜순으로 표시
- 로딩, 빈 결과, 입력 오류 및 서비스 오류 상태 안내
- 데스크톱과 모바일 웹 지원

### 3.2 제외 범위

- 조식과 석식 조회
- 두 학교의 급식 비교 또는 평가
- 회원가입, 로그인, 즐겨찾기 및 알림
- 급식 데이터의 수정, 저장 또는 사용자 제보
- 영양 성분에 대한 의학적 판단이나 추천

앱 이름에는 "배틀"이 포함되지만, 학교 간 비교 기능은 이번 범위에 포함하지
않는다.

## 4. 핵심 사용자 흐름

### 4.1 1단계: 학교 검색 및 선택

1. 사용자가 학교 이름의 일부를 입력한다.
2. 사용자가 검색을 실행한다.
3. 앱은 학교명, 지역, 학교 종류와 주소를 포함한 결과를 표시한다.
4. 사용자가 한 학교를 선택하면 다음 단계가 활성화된다.

### 4.2 2단계: 날짜 범위 선택

1. 앱은 시작일과 종료일 입력을 제공한다.
2. 사용자가 유효한 날짜 범위를 선택한다.
3. 선택한 학교와 날짜 범위를 확인한 뒤 조회를 실행한다.

### 4.3 3단계: 날짜별 급식 결과

1. 앱은 선택한 기간의 중식 정보만 조회한다.
2. 결과는 날짜 오름차순으로 표시한다.
3. 각 날짜에는 메뉴를 필수로 표시하고, 제공되는 경우 칼로리와 영양 정보를
   함께 표시한다.
4. 사용자는 학교 또는 날짜를 변경해 다시 조회할 수 있다.

## 5. 기능 요구사항

| ID | 요구사항 |
|---|---|
| FR-01 | 사용자는 학교 이름의 일부로 학교를 검색할 수 있어야 한다. |
| FR-02 | 공백만 있는 검색어는 요청하지 않고 입력 오류를 표시해야 한다. |
| FR-03 | 검색 결과에는 학교명, 시도교육청 또는 지역, 학교 종류와 주소를 표시해야 한다. |
| FR-04 | 사용자는 검색 결과 중 정확히 한 학교를 선택할 수 있어야 한다. |
| FR-05 | 학교 선택 전에는 급식 조회를 실행할 수 없어야 한다. |
| FR-06 | 사용자는 시작일과 종료일을 선택할 수 있어야 한다. |
| FR-07 | 시작일이 종료일보다 늦으면 조회하지 않고 수정 방법을 안내해야 한다. |
| FR-08 | 한 번에 조회할 수 있는 날짜 범위는 시작일과 종료일을 포함해 최대 31일로 제한한다. |
| FR-09 | 앱은 선택한 학교와 기간의 중식만 조회해야 한다. |
| FR-10 | 급식 결과는 날짜 오름차순으로 표시해야 한다. |
| FR-11 | 각 결과에는 날짜와 메뉴를 표시하고, 값이 있는 경우 칼로리와 영양 정보를 표시해야 한다. |
| FR-12 | 사용자는 이전 단계로 돌아가 학교나 날짜를 변경할 수 있어야 한다. |
| FR-13 | 요청 처리 중에는 중복 요청을 방지하고 현재 처리 상태를 표시해야 한다. |
| FR-14 | 프론트엔드는 NEIS API를 직접 호출하지 않고 백엔드 API만 호출해야 한다. |

## 6. 상태 및 예외 처리

| 상황 | 사용자 경험 |
|---|---|
| 검색 결과 없음 | "검색 결과가 없습니다"와 함께 학교명 또는 지역을 확인하도록 안내한다. |
| 학교 미선택 | 날짜 입력과 조회 동작을 비활성화하고 학교를 먼저 선택하도록 안내한다. |
| 잘못된 날짜 범위 | 문제가 있는 날짜 입력 가까이에 원인과 수정 방법을 표시한다. |
| 31일 초과 | 조회 가능한 최대 기간이 31일임을 알리고 조회를 실행하지 않는다. |
| 급식 정보 없음 | 오류 화면 대신 선택 기간에 등록된 중식이 없다는 빈 상태를 표시한다. |
| 네트워크 또는 서버 오류 | 이해하기 쉬운 오류 메시지와 다시 시도 동작을 제공한다. |
| NEIS 인증 또는 호출 한도 오류 | 사용자 입력 문제로 표현하지 않고 현재 조회 서비스를 사용할 수 없음을 알린다. |
| 일부 선택값 변경 | 학교를 변경하면 기존 급식 결과를 초기화해 이전 학교의 결과가 남지 않게 한다. |

오류는 색상만으로 구분하지 않으며, 사용자가 취할 수 있는 다음 행동을 텍스트로
제공한다.

## 7. UI 및 사용성 요구사항

UI는 참조 세션에서 권장한 **Linear에서 영감을 받은 스타일**로 구성한다.
Linear 제품을 복제하지 않고 다음 특성을 이 앱의 3단계 흐름에 적용한다.

- 장식보다 정보 구조와 빠른 조작을 우선하는 간결한 화면
- 중립색 중심의 절제된 색상과 핵심 행동에만 사용하는 단일 강조색
- 명확한 제목 단계, 약 16px의 본문 크기와 읽기 쉬운 행간
- 4px 또는 8px 기반의 일관된 간격과 얇은 경계선
- 과도한 그림자, 글래스모피즘 및 무의미한 애니메이션 배제
- 진행 중인 단계와 완료한 단계를 분명히 보여주는 3단계 표시
- 높은 정보 밀도를 유지하되 관련 정보는 묶고 단계 간에는 충분한 여백 제공
- 학교 검색 입력에서 `Enter`로 검색하고 검색 결과를 키보드로 이동·선택 가능
- 각 화면에서 가장 중요한 행동 하나만 기본 버튼으로 강조

### 7.1 접근성

- WCAG 2.2 AA 수준을 목표로 일반 텍스트 명암비 4.5:1 이상을 유지한다.
- 모든 기능은 키보드로 이용할 수 있고 현재 포커스가 명확히 보여야 한다.
- 입력에는 시각적 레이블을 제공하고 오류 메시지를 입력과 연결한다.
- 주요 터치 대상은 최소 44x44px을 권장한다.
- 320px 화면 폭과 200% 확대에서도 핵심 흐름을 완료할 수 있어야 한다.
- 상태 변화와 로딩 상태는 보조 기술에 전달되어야 한다.
- 모션을 사용하는 경우 `prefers-reduced-motion` 설정을 존중한다.

### 7.2 반응형 동작

- 넓은 화면에서는 단계와 결과를 읽기 쉬운 최대 너비 안에 배치한다.
- 좁은 화면에서는 입력, 버튼 및 결과 카드를 한 열로 배치한다.
- 급식 메뉴가 길어도 가로 스크롤 없이 읽을 수 있어야 한다.

## 8. 비기능 요구사항

- API 키와 내부 오류 상세는 사용자 화면이나 브라우저 번들에 노출하지 않는다.
- 사용자가 재시도할 수 있도록 일시적인 외부 API 실패와 입력 오류를 구분한다.
- 주요 화면은 일반적인 모바일 및 데스크톱 최신 브라우저에서 동작해야 한다.
- 성능 목표는 LCP 2.5초 이하, INP 200ms 이하, CLS 0.1 이하로 한다.
- 날짜와 사용자 안내 문구는 한국어 기준으로 표시한다.

## 9. 제품 인수 조건

- 앱 제목과 목적, 대상 사용자가 화면 및 문서의 정의와 일치한다.
- 부분 학교명 검색으로 학교를 선택할 수 있다.
- 유효한 날짜 범위에 대해 중식 기준의 날짜별 메뉴가 표시된다.
- 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음 상태가 서로 구분된다.
- 프론트엔드에서 NEIS API 또는 NEIS API 키를 직접 사용하지 않는다.
- Linear에서 영감을 받은 UI 원칙과 접근성 요구사항을 핵심 흐름에 적용한다.

